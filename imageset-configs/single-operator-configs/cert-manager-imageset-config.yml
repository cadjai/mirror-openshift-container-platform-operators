kind: ImageSetConfiguration
apiVersion: mirror.openshift.io/v2alpha1
mirror:
  operators:
  - catalog: registry.redhat.io/redhat/community-operator-index:v4.20
    targetCatalog: cert-manager
    targetTag: v4.20
    packages:
    - name: cert-manager
      channels:
      - name: "stable"

