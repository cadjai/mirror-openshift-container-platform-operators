kind: ImageSetConfiguration
apiVersion: mirror.openshift.io/v2alpha1
mirror:
  operators:
  - catalog: registry.redhat.io/redhat/certified-operator-index:v4.20
    targetCatalog: ovms-operator
    targetTag: v4.20
    packages:
    - name: ovms-operator 
      channels:
      - name: "alpha"

