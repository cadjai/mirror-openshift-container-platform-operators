kind: ImageSetConfiguration
apiVersion: mirror.openshift.io/v1alpha2
mirror:
  operators:
  - catalog: registry.redhat.io/redhat/certified-operator-index:v4.12
    targetName: ovms-operator
    targetTag: v4.12
    packages:
    - name: ovms-operator 
      channels:
      - name: "alpha"

