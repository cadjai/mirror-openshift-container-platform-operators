kind: ImageSetConfiguration
apiVersion: mirror.openshift.io/v2alpha1
mirror:
  operators:
  - catalog: registry.redhat.io/redhat/redhat-operator-index:v4.20
    targetCatalog: cluster-logging
    targetTag: v4.20
    packages:
    - name: cluster-logging
      channels:
      - name: "stable-5.7"
      - name: "stable-5.8"

