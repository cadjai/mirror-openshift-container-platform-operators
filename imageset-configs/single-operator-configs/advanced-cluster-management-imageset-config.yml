kind: ImageSetConfiguration
apiVersion: mirror.openshift.io/v2alpha1
mirror:
  operators:
  - catalog: registry.redhat.io/redhat/redhat-operator-index:v4.20
    targetCatalog: advanced-cluster-management
    targetTag: v4.20
    packages:
    - name: advanced-cluster-management
      channels:
      - name: "release-2.9"

